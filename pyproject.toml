[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "noctis"
version = "0.1.0"
description = "noctis"
dependencies = [
    "torch",
    "pandas",
    "numpy",
]

[tool.setuptools.packages.find]
where = ["src"]